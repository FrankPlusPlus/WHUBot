name: bot
channels:
  - conda-forge
  - defaults

# Generated from the active 'bot' conda environment (pinned to installed versions)
dependencies:
  - python=3.10.19
  - numpy=1.24.4
  - scipy=1.15.2
  - pandas=2.3.3
  - pyyaml=6.0.3
  - matplotlib=3.10.8
  - scikit-image=0.25.2
  - pillow=12.0.0
  - imageio=2.37.0
  - scikit-image=0.25.2
  - pywavelets=1.8.0
  - tifffile=2025.2.18
  - python-dateutil=2.9.0
  - pip=25.3
  - pip:
    - roboticstoolbox-python==1.1.1
    - spatialmath-python==1.1.15
    - rtb-data==1.0.1
    - pgraph-python==0.6.3
    - swift-sim==1.1.0
    - roboticstoolbox-python==1.1.1
    - spatialmath-python==1.1.15

# Notes:
# - Some packages (system libs and low-level BLAS/LAPACK etc.) are managed by conda channels and
#   will be resolved automatically when creating the environment from this file.
# - If you want a narrower reproduction (exact build), consider exporting with `conda env export --from-history` or `conda list --explicit`.
